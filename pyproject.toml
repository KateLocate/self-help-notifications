[project]
name = "self-help-notifications"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "pymongo[srv]>=4.16.0",
    "uvicorn>=0.40.0",
    "websockets>=16.0",
]
